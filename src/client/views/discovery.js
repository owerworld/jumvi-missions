import {el,heading} from '../components/dom.js';import {button} from '../components/button.js';
export default function view({ui,state,send,catalog={missions:[]}}){const tr=state.locale==='tr',s=el('section');s.dataset.view='discovery';s.append(button(ui.back,()=>send('BACK'),{kind:'link'}),heading(ui.titles.discovery),el('p',tr?'Oyuna uygun görevin açıklamasını aç.':'Open the instructions for a suitable mission.'));const list=el('ul',undefined,'mission-list');for(const m of catalog.missions){const li=el('li');li.append(el('h2',m.titles[state.locale]),el('p',tr?`${m.players} oyuncu · ${m.equipment.paddles} paddle · ${m.equipment.balls} top`:`${m.players} players · ${m.equipment.paddles} paddles · ${m.equipment.balls} ball`),button(tr?'Görevi aç':'Open mission',()=>send('SELECT_MISSION',{missionId:m.id})));li.dataset.missionId=m.id;list.append(li);}s.append(list,button(ui.back,()=>send('BACK'),{kind:'link'}));return s;}
