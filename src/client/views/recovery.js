import {el,heading} from '../components/dom.js';import {button} from '../components/button.js';
export default function({state,ui,cp,send}){const tr=state.locale==='tr',s=el('section');s.dataset.view='recovery';s.append(heading(tr?'Oyun alanını kontrol et':'Check the play area'),el('p',cp?.safe||ui.askText));if(state.round?.state==='active')s.append(button(tr?'Turu durdur':'Stop this round',()=>send('STOP'),{active:true}));s.append(button(tr?'Yetişkinden yardım al':'Ask a grown-up',()=>send('ASK')),button(tr?'Görev açıklaması':'Mission instructions',()=>send('HELP')));return s;}
