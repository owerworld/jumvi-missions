# M1 — semantic shell and state boundary

LOCK01–09 unchanged. M0 closed at ade4921; production unchanged.

17 view modules map in locked order to entry/discovery/help/active/interrupted/stopped/report/attribution/record-result/guest/group/availability/recovery/adult/management/returning/new-player. At this milestone these are skeletons. No real personal input or persistence (R3). No claim that later milestones are implemented.

Pure transitions require document and revision. Stop preserves round identity; resume never manufactures a result. A stale event cannot start a second round. Timer/media/reconnect are not recognized as result actions. Native controls use click activation, pointer cancellation and repeat-key prevention; state change focuses context heading, never new Resume button. No global debounce.

LOCK06 JSON copied verbatim and CSS tokens generated without editing the source. Child 56, active64, adult48 min targets in rem; text grows. Reduced motion disables transition. Native button, radio, link and modal semantics. No legacy app.js/3D/XP/timer/auto-profile in public graph.

Deterministic dist allowlist, immutable release-path directory for app/content, separate EN/TR shells. Local static test gateway runs staging adapter; no SW registration before M5. Original production Worker and its config untouched. M0 staging adapter now serves the new static shells without legacy TR injection; only ASSETS binding.

Acceptance: Node state/a11y/isolation contracts; Chromium and WebKit shell/17-view/focus/reload smoke. Real children, outdoor, device AT and final UI acceptance not represented by this gate.
