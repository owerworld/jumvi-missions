# M6 pilot revision 2 — human review required

Baseline: e63cf80f0b444062401c3d268b2992f71f45a8fd. Local only. No push or deploy.

- Responsive control tracks use minmax(0,1fr). Minimum 20px horizontal control padding is preserved as a fixed inset when text is enlarged; typography and minimum target sizes are unchanged. Labels grow vertically. Discretionary Turkish/English syllable breaks retain the original accessible names.
- The existing text-size-sensitive compact state also stacks active art and instruction text. No clipping, scale, fixed action bar or hidden information.
- Browser history restores document-local entry/discovery/help route and reading positions. The route map contains no personal records and never replays rounds or reports. Native history controls remain usable. Round traversal retains the interruption boundary.
- m25 and m07 presentation records and art are unchanged. They have no new human acceptance.
- m20 v2 recomposes approved actor groups into two separate moments: A passes to B (one ball stuck on B's blue front), then A moves behind C (ball remains with B). Solid flight route and dashed ground route are separate. No pose/product mirroring or new rule.
- m30 v2 shows caller/thrower A and catcher B together. RIGHT is B's own right; the image ends at blue-front contact. Existing detach/neutral-return instructions remain separate in Help. No mirroring.
- Entry overlays for m20/m30 remove equipment/annotation repetition without changing canonical records, required rules or safety text. No new design tokens or screen hierarchy.
- Source SVGs are separately versioned under artifacts/presentation-pilot-v2; only WebP derivatives are distributed. All prior sources remain unchanged. Every new asset remains a HUMAN_REVIEW_REQUIRED presentation candidate.

Automated checks and desktop browser screenshots do not constitute child/field/device validation or M6 PASS. Native menu 200% browser zoom remains open while the Mac is locked.
