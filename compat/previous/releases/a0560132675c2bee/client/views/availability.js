import {el,heading} from '../components/dom.js';import {button} from '../components/button.js';
export default function({state,ui,send}){const tr=state.locale==='tr',s=el('section');s.dataset.view='availability';s.append(heading(tr?'Açıklama eksik':'Instructions unavailable'),el('p',tr?'Oyunun nasıl oynanacağını gösteren gerekli içerik şu anda hazır değil.':'The required instructions are unavailable right now.'));s.append(button(tr?'Yeniden yükle':'Retry',()=>send('RETRY_MISSION')),button(tr?'Hazır görevleri gör':'View missions',()=>send('GO',{screen:'discovery'})),button(tr?'Ayrıl':'Leave',()=>send('LEAVE'),{kind:'link'}),button(tr?'Yardım':'Help',()=>send('ASK'),{kind:'link'}));return s;}
