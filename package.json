{
  "name": "jumvi-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node tools/build-app.mjs",
    "test": "node --test tests/state/*.test.mjs tests/a11y/*.test.mjs tests/isolation/*.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
