import http from 'node:http';import {readFileSync,existsSync,statSync} from 'node:fs';import {resolve,extname} from 'node:path';import {pathToFileURL} from 'node:url';import worker from '../src/m0-staging-worker.mjs';
const root=resolve('dist');const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json','.webp':'image/webp','.png':'image/png','.woff2':'font/woff2','.ttf':'font/ttf','.svg':'image/svg+xml'};
export async function serve(port=8919){const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url,'https://jumvi-missions-staging.saykirtasiye.workers.dev');const response=await worker.fetch(new Request(u,{method:req.method,headers:req.headers}),{ASSETS:{fetch:async r=>{let p=new URL(r.url).pathname;if(p==='/')p='/index.html';const f=resolve(root,'.'+decodeURIComponent(p));if(!f.startsWith(root+'/')||!existsSync(f)||!statSync(f).isFile())return new Response('Not found',{status:404});return new Response(readFileSync(f),{headers:{'Content-Type':mime[extname(f)]||'application/octet-stream'}});}}});res.writeHead(response.status,Object.fromEntries(response.headers));res.end(Buffer.from(await response.arrayBuffer()));}catch(e){res.writeHead(500);res.end('Local test server error');}});await new Promise(ok=>server.listen(port,'127.0.0.1',ok));return server;}
if(import.meta.url===pathToFileURL(process.argv[1]).href){await serve(Number(process.env.PORT||8919));console.log('JUMVI local isolated app: http://127.0.0.1:8919');}
